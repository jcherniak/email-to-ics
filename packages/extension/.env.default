# Email-to-ICS Extension Defaults
#
# Configuration is applied in three tiers (later entries override earlier ones):
#   1. .env.default (this file, checked into git) → project-wide defaults
#   2. .env         (not committed)              → machine / developer overrides
#   3. Settings UI  (at runtime)                 → per-user / per-session overrides
#
# To change a value, you can either:
#   - Copy the relevant line into your local .env file and edit it there, or
#   - Adjust the setting from the Email-to-ICS Settings dialog in the UI.
#
# DEFAULT_TIMEZONE controls the timezone used when parsing and generating events.
# It should be set to an IANA timezone identifier, e.g. "America/Los_Angeles" or "Europe/Berlin".
#
# CUSTOM_PROMPT allows you to customize the system prompt used when interpreting emails.
# If left unset, the extension will fall back to its built-in default prompt.
# You can override it here, in .env, or via the Settings UI.

DEFAULT_TIMEZONE=America/Los_Angeles
# Example:
# DEFAULT_TIMEZONE=Europe/Berlin

# Uncomment and customize to define a default custom prompt:
# CUSTOM_PROMPT=You are an assistant that converts emails into calendar events. Be concise and accurate.
