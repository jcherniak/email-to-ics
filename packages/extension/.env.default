# Email-to-ICS Extension Defaults
# Override locally by creating a .env file (not committed to git)
# These can also be overridden at runtime via the Settings dialog

DEFAULT_TIMEZONE=America/Los_Angeles
